import { DaysCheckerPipe } from './days-checker.pipe';

describe('DaysCheckerPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysCheckerPipe();
    expect(pipe).toBeTruthy();
  });
});
